/**
 * Send project to the server. FIXME: not actually implemented yet.
 * @async
 * @param {object} project Values from the project form
 * @return {Promise}
 */
export const saveProject = project => new Promise(resolve => resolve({ ...project, id: 132 }));
