import FileUploadDialog from './FileUploadDialog';
import reducer, { NAME, actions, actionTypes } from './FileUploadDialog.ducks';

export { reducer, NAME, actions, actionTypes };
export default FileUploadDialog;
