/**
 * Send plan to the server. FIXME: not really implemented yet
 * @async
 * @return {Promise}
 */
export const savePlan = () => new Promise((resolve, reject) => reject(new Error('PlanForm error!')));
