import PlanVersionHistory from './PlanVersionHistory';
import reducer, { NAME, actionTypes, actions } from './PlanVersionHistory.ducks';

export { NAME, actions, reducer, actionTypes };
export default PlanVersionHistory;
